[
  {
    "canonical_name": "Tensor.register_hook",
    "failure_category": "TEST_BUG",
    "root_cause_summary": "The test expects register_hook(None) to fail at registration time, but current PyTorch accepts the hook and only raises TypeError when backward later tries to call the stored None hook."
  },
  {
    "canonical_name": "torch.library.Library",
    "failure_category": "PYTORCH_BUG",
    "root_cause_summary": "Creating torch.library.Library with kind=\"FRAGMENT\" and a non-empty dispatch key triggers an internal assert in /pytorch/aten/src/ATen/core/library.cpp:87; the same failure reproduces with CPU and PrivateUse1, so this points to PyTorch core registration logic."
  },
  {
    "canonical_name": "torch.nn.Module.buffers",
    "failure_category": "TEST_BUG",
    "root_cause_summary": "The invalid-self test is too strict: calling the unbound method with a string deterministically raises AttributeError from self.named_buffers, not TypeError."
  },
  {
    "canonical_name": "torch.nn.Module.modules",
    "failure_category": "TEST_BUG",
    "root_cause_summary": "The invalid-self test is too strict: calling the unbound method with a string deterministically raises AttributeError from self.named_modules, not TypeError."
  },
  {
    "canonical_name": "torch.nn.Module.named_modules",
    "failure_category": "TEST_BUG",
    "root_cause_summary": "The invalid-self test is too strict: calling the unbound method with a string deterministically raises AttributeError when the implementation accesses self._modules, not TypeError."
  },
  {
    "canonical_name": "torch.nn.Module.named_parameters",
    "failure_category": "TEST_BUG",
    "root_cause_summary": "The invalid-self test is too strict: calling the unbound method with a string deterministically raises AttributeError from self._named_members, not TypeError."
  },
  {
    "canonical_name": "torch.nn.Module.register_forward_hook",
    "failure_category": "TEST_BUG",
    "root_cause_summary": "The test expects register_forward_hook(None) to fail during registration, but current PyTorch stores None and raises TypeError only when forward later invokes the hook."
  },
  {
    "canonical_name": "torch.nn.Module.register_forward_pre_hook",
    "failure_category": "TEST_BUG",
    "root_cause_summary": "The test expects register_forward_pre_hook(None) to fail during registration, but current PyTorch stores None and raises TypeError only when forward later invokes the hook."
  },
  {
    "canonical_name": "torch.nn.Module.register_load_state_dict_post_hook",
    "failure_category": "TEST_BUG",
    "root_cause_summary": "The test expects register_load_state_dict_post_hook(None) to fail during registration, but current PyTorch stores None and raises TypeError only when load_state_dict later invokes the hook."
  },
  {
    "canonical_name": "torch.utils.swap_tensors",
    "failure_category": "TEST_BUG",
    "root_cause_summary": "The invalid-input test is too strict: passing a non-Tensor reaches t._use_count() and raises AttributeError, while the test only accepts TypeError or RuntimeError."
  },
  {
    "canonical_name": "torch.autograd._unsafe_preserve_version_counter",
    "failure_category": "TEST_BUG",
    "root_cause_summary": "The invalid-input test is too strict: passing None hits the constructor assertion that tensors must be a tuple and raises AssertionError, while the test only accepts TypeError or RuntimeError."
  },
  {
    "canonical_name": "torch.fx.node.has_side_effect",
    "failure_category": "INSUFFICIENT_COVERAGE",
    "root_cause_summary": "The case is skipped because this API has no meaningful NPU device behavior to validate, so the generated file does not provide a runnable NPU-focused coverage scenario."
  },
  {
    "canonical_name": "torch.autograd.graph._MultiHandle",
    "failure_category": "TEST_BUG",
    "root_cause_summary": "The test expects _MultiHandle(None) to fail in the constructor, but the constructor accepts None and TypeError appears only if remove() later iterates the handles."
  }
]
